join array key = {
    index = 1; result = stdlib/list/index.pvd array 0;
    while @(less-than index (len array)) @{
        result = concat result key (stdlib/list/index.pvd array index);
        index = + index 1;
        result
    }
}
