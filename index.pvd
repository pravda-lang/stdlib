index list spec = {
    index = 0;
    result = (while @(less-than index spec) @{
        list = cdr list;
        index = + index 1;
        list
    });
    if (equal result null) @{
        car list
    } @{
        car result
    }
}
